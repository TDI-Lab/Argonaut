server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — API calls go directly to the backend URL baked into the JS bundle
    location / {
        try_files $uri $uri/ /index.html;
    }
}
